import { AqiToColorPipe } from './aqi-to-color.pipe';

describe('AqiToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AqiToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
